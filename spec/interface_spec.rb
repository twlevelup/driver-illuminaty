require "interface"

describe Interface do

#	it "accept valid commands" do
#		valid_commands = "MLMRM"
#		expect(Interface.commands_valid? valid_commands).to eq(true)
#	end
#	it "reject invalid commands" do
#		invalid_commands = "MDM"
#		expect(Interface.commands_valid? invalid_commands).to eq(false)
#	end
end